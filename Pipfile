[[source]]
name = "pypi"
url = "https://pypi.org/weblinks"
verify_ssl = true

[dev-packages]
coveralls = ">=1.5.1"
pytest = ">=4.0.2"
pytest-cov = ">=2.6.0"
tox = ">=3.7.0"

[packages]
validators = ">=0.18.2"

[requires]
python_version = "3.7"